import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    input: {
      fontSize: 36,
      textAlign: 'right',
      marginBottom: 20,
      borderBottomWidth: 1,
      borderColor: '#ccc',
      padding: 10,
    },
});

export default styles;